---
# Configure LAG L3 interfaces

- name: Configure LAG interfaces (default VRF) - IPv4
  arubanetworks.aoscx.aoscx_l3_interface:
    interface: "{{ item.interface_name }}"
    ipv4:
      - "{{ item.address }}"
    vrf: default
  loop: "{{ l3_interfaces.lag_default_vrf | selectattr('address', 'match', '^\\d+\\.\\d+\\.\\d+\\.\\d+/') | list }}"
  loop_control:
    label: "Interface: {{ item.interface_name }} IP: {{ item.address }}"
  when: l3_interfaces.lag_default_vrf | length > 0

- name: Configure LAG interfaces (default VRF) - IPv6
  arubanetworks.aoscx.aoscx_l3_interface:
    interface: "{{ item.interface_name }}"
    ipv6:
      - "{{ item.address }}"
    vrf: default
  loop: "{{ l3_interfaces.lag_default_vrf | rejectattr('address', 'match', '^\\d+\\.\\d+\\.\\d+\\.\\d+/') | list }}"
  loop_control:
    label: "Interface: {{ item.interface_name }} IP: {{ item.address }}"
  when: l3_interfaces.lag_default_vrf | length > 0

- name: Configure LAG interfaces (custom VRF) - IPv4
  arubanetworks.aoscx.aoscx_config:
    lines:
      - vrf attach {{ interface_vrf }}
      - ip address {{ item.address }}
    parents: interface {{ item.interface_name | replace('lag', 'lag ') }}
    match: line
  loop: "{{ l3_interfaces.lag_custom_vrf | selectattr('address', 'match', '^\\d+\\.\\d+\\.\\d+\\.\\d+/') | list }}"
  loop_control:
    label: "Interface: {{ item.interface_name }} IP: {{ item.address }} Interface VRF: {{ interface_vrf }}"
  when: l3_interfaces.lag_custom_vrf | length > 0
  vars:
    ansible_connection: "{{ aoscx_connection_type }}"
    # Get VRF from the interface configuration, not from IP address
    interface_vrf: "{{ item.interface.vrf.name if item.interface.vrf is defined and item.interface.vrf.name is defined else 'default' }}"

- name: Configure LAG interfaces (custom VRF) - IPv6
  arubanetworks.aoscx.aoscx_config:
    lines:
      - vrf attach {{ interface_vrf }}
      - ipv6 address {{ item.address }}
    parents: interface {{ item.interface_name | replace('lag', 'lag ') }}
    match: line
  loop: "{{ l3_interfaces.lag_custom_vrf | rejectattr('address', 'match', '^\\d+\\.\\d+\\.\\d+\\.\\d+/') | list }}"
  loop_control:
    label: "Interface: {{ item.interface_name }} IP: {{ item.address }} Interface VRF: {{ interface_vrf }}"
  when: l3_interfaces.lag_custom_vrf | length > 0
  vars:
    ansible_connection: "{{ aoscx_connection_type }}"
    # Get VRF from the interface configuration, not from IP address
    interface_vrf: "{{ item.interface.vrf.name if item.interface.vrf is defined and item.interface.vrf.name is defined else 'default' }}"
