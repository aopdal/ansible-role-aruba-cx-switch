# GitHub Pages Documentation Deployment
#
# COMMENTED OUT: GitHub Pages requires either:
# - A public repository, OR
# - GitHub Pro/Team/Enterprise plan
#
# For private repos on the free plan, use local documentation:
#   make docs-serve    # Live preview at http://127.0.0.1:8000
#   make docs-build    # Build static site locally
#
# Uncomment this workflow when the repo becomes public or you have a paid plan.

# name: Deploy Documentation
#
# on:
#   push:
#     branches:
#       - main
#     paths:
#       - 'docs/**'
#       - 'README.md'
#       - 'CONTRIBUTING.md'
#       - 'CHANGELOG.md'
#       - 'mkdocs.yml'
#       - '.github/workflows/docs.yml'
#   workflow_dispatch:
#
# permissions:
#   contents: write
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0  # Fetch all history for git-revision-date-localized plugin
#
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.11'
#
#       - name: Cache dependencies
#         uses: actions/cache@v3
#         with:
#           path: ~/.cache/pip
#           key: ${{ runner.os }}-pip-${{ hashFiles('requirements-docs.txt') }}
#           restore-keys: |
#             ${{ runner.os }}-pip-
#
#       - name: Install dependencies
#         run: |
#           pip install -r requirements-docs.txt
#
#       - name: Build and deploy documentation
#         run: mkdocs gh-deploy --force
