---
# requirements.yml
# Collections required for the role
collections:
  - name: arubanetworks.aoscx
    version: ">=4.5.1"

  - name: netbox.netbox
    version: ">=3.23.0"

  - name: ansible.utils
    version: ">=2.0.0"

  - name: community.docker
    version: ">=3.10.2"

  - name: ansible.posix
    version: ">=1.4.0"
