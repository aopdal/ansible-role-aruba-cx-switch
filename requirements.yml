---
# requirements.yml
# Collections required for the role
collections:
  - name: arubanetworks.aoscx
    version: ">=4.4.0"

  - name: netbox.netbox
    version: ">=3.21.0"

  - name: ansible.utils
    version: ">=2.0.0"
