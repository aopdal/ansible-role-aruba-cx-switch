# ZTP (Zero Touch Provisioning)

> **Deprecated:** ZTP support has been removed from this role. It did not work correctly and is better maintained as a separate project.

ZTP functionality is no longer part of `ansible-role-aruba-cx-switch`. If you were using the `aoscx_generate_ztp_config` variable, it has no effect and can be removed from your configuration.
